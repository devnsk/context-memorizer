---
description: Finish current task and save context before stopping
---

When you are done with a task or blocked:

1. Auto-save the context (intent, decisions, next steps) using `devctx save --auto`.
2. Notify the user.

// turbo
```bash
devctx save --auto
```
